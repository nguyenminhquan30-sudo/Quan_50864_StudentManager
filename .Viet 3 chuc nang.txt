vector<Student> listStudent;

// ============================
//   ADD + UPDATE + DELETE
// ============================
void addStudent() {
    string id, name;
    int age;

    cout << "\n--- Add Student ---\n";
    cout << "Enter ID: ";
    cin >> id;

    cout << "Enter Name: ";
    cin.ignore();
    getline(cin, name);

    cout << "Enter Age: ";
    cin >> age;

    listStudent.push_back(Student(id, name, age));
    cout << "Student added successfully!\n";
}

void updateStudent() {
    string id;
    cout << "\n--- Update Student ---\n";
    cout << "Enter ID to update: ";
    cin >> id;

    for (auto &st : listStudent) {
        if (st.id == id) {
            cout << "New Name: ";
            cin.ignore();
            getline(cin, st.name);

            cout << "New Age: ";
            cin >> st.age;

            cout << "Updated successfully!\n";
            return;